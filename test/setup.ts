// Configuração para testes com Bun
// @ts-ignore - módulos disponíveis em runtime
import { afterEach } from "bun:test";

// Configurações básicas para testes
afterEach(() => {
  // Cleanup após cada teste
  // Em tempo de execução, a limpeza será feita corretamente
});

export {};
